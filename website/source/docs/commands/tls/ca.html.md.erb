---
layout: "docs"
page_title: "Commands: TLS CA Create"
sidebar_current: "docs-commands-tls-ca"
---

# OpenGyoza TLS CA Create

Command: `gyoza tls ca create`

This command create a self signed CA to be used for OpenGyoza TLS setup.

## Example

Create CA:

```bash
$ gyoza tls ca create
==> Saved consul-ca.pem
==> Saved consul-ca-key.pem
```

## Usage
Usage: `gyoza tls ca create [filename-prefix] [options]`

#### TLS CA Create Options

- `-days=<int>` - Provide number of days the CA is valid for from now on, defaults to 5 years.